# Copy to gradle.properties.local for machine-specific overrides (gitignored).

# autoVersionBump=true
# skipVersionBump=false
# presenceApiBase=https://dupeclient-presence.vercel.app/api/client/presence

# Per-branch MC settings live in gradle.properties on each version branch:
#   1.21.11 (default) | 1.21.10 | 26.1
